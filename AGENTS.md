# guoyang-skill 维护约定

## 项目边界

- 本仓库只维护 `guoyang-skill` 技能、确定性辅助脚本及其文档。
- `../guoyang-pro/` 是独立项目；本项目可以调用其公开 CLI，但不得顺带修改其源码、数据或发布配置。
- `spec.md` 是产品需求来源。实现变化应保持与其中“条件扩展、实际查询、HTML 报告、历史对比、验证”目标一致。

## 技能结构

- `SKILL.md` 只保留触发范围、核心流程和关键约束。
- 条件扩展规则与报告契约放在 `references/`，按需读取。
- 可重复且容易出错的逻辑放在 `scripts/`；脚本使用 Node.js 18+ 内置模块，不新增运行时依赖。
- `agents/openai.yaml` 的名称、描述和默认提示词应与 `SKILL.md` 一致。

## 行为约束

- 每轮岗位检索必须实际调用 `@neruzz-jpg/guoyang-pro` CLI；不得凭模型常识伪造岗位。
- 报告必须保留查询覆盖状态、岗位来源、抓取时间和风险提示；零结果不得表述为“全网无岗位”。
- 面向用户的 HTML 只展示岗位清单和投递所需信息；画像、策略、查询轨迹、覆盖
  状态、历史比较和机器数据写入旁路日志，不嵌入 HTML。
- 版本化扩展是候选库和兜底；模型可以自主选择、组合或补充检索，但每条查询必须说明目的，并通过脚本的参数与预算校验。
- 专业树父子关系只表示分类包含，不推导兄弟节点相似；跨专业检索只能走带关系类型、
  权重和理由的显式边。向量相似度仅可用于候选起点召回。
- 模型可以自主取舍、分档和撰写报告，但只能引用 CLI 返回的事实和 stableKey，不得修改岗位字段或伪造来源。
- 历史报告中“本轮未再检出”不等于岗位已关闭；只有官方来源明确时才能断言关闭。
- 不把用户简历、证件号、手机号、账号密码等敏感信息写入画像、查询计划或报告。

## 验证

完成改动后至少运行：

```bash
npm test
python3 "${CODEX_HOME:-$HOME/.codex}/skills/.system/skill-creator/scripts/quick_validate.py" .
```

如果修改了查询执行器，还应使用 mock CLI 或明确获准的真实网络查询验证；测试不得默认访问生产招聘源。
