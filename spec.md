# 项目概览
本项目为使用guoyang-pro cli的技能，帮助用户找到适合自己的国央企招聘岗位与信息。

# 项目现状
对cli的能力摸底发现，cli仅支持简单的岗位查询，需要ai自行发散查询范围。
现有提示词如下：
```
请先运行 `npx @neruzz-jpg/guoyang-pro@latest help` 了解可用命令，然后帮我规划国央企求职。

开始前，请依次询问我的学历、专业、院校层级、意向城市、意向行业、招聘类型和关键词。

然后请：
1. 查询企业名录、在招岗位和招聘时间线；
2. 如果一次查询没有结果，适当放宽条件继续寻找；
3. 对岗位按匹配程度、截止时间和投递价值进行比较；
4. 输出“优先投递 / 可以尝试 / 继续观察”三档清单并说明理由；
5. 保留岗位来源、截止时间和投递入口，提醒我以官方页面为准。
```

# 优化点：
1. 某个专业，例如“新闻传播”，其实可以被多种不同专业和岗位覆盖，比如文科大类、不限、新闻、传播、编辑等等。因此ai在收到专业时，需要首先发散为多种相关专业。
2. 对于某个地区，也是类似。比如四川，实际上可以被多个城市覆盖，比如成都、绵阳、绵阳等。因此ai在收到地区时，需要首先发散为多种相关城市。
3. 其余字段的发散，也需要考虑。
4. 发散的逻辑，尽可能不依赖ai，避免多次发起查询时返回结果不一致。
5. 每次查询后，需要生成人类可读的报告。建议使用html格式。因此在skill中需要规定该报告的参数和格式，并包含验证的逻辑。
6. 多次查询时，后面的查询可以参考前面的查询结果（这也要求ai前序的html报告不仅人类可读，也有良好的代码可解析性）。当然，每次查询都需要实际进行发起，只是后续的报告可以对前序报告进行一些检查， 比如之前推荐的岗位是否有信息变更等等。

# 后续方向补充

检索和报告不应被固定规则完全替代：

- 确定性专业、地区和行业映射只作为 AI 的候选库与兜底；
- AI 应提出检索假设，自主选择条件组合，并根据前轮结果决定是否收窄、扩展或停止；
- AI 应自主判断岗位取舍、分档、风险、行动建议和报告叙事；
- 代码继续负责真实 CLI 调用、查询预算、参数合法性、来源留痕、历史事实对比与 HTML 校验；
- AI 不得虚构岗位、修改 CLI 返回的事实或把主观判断写成官方结论。

# 专业树与报告展示补充

- 专业扩展采用树形层级，例如“专业不限 → 文科/人文社科 → 文学 →
  中国语言文学类 → 汉语言文学”。
- 对输入专业进行有宽度、深度和候选上限的 Beam 扫描；逐级父类用于放宽，
  跨专业只走显式关联边。
- 专业树输出路径和候选分数供 AI 制定检索策略，不要求机械执行所有候选。
- 同一父节点不等于求职相似或资格兼容，Beam 不默认扫描兄弟节点。
- 向量化只作为未知专业的可选起点召回，不能自动创建专业关系或替代公告资格判断。
- 用户 HTML 只展示岗位列表和投递所需信息。
- 画像、专业树路径、AI 策略、查询轨迹、覆盖状态、历史差异与机器数据写入独立
  日志，不再嵌入 HTML。
